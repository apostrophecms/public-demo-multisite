export default {
  local: 'localhost:3000',
  // Should be a real registered domain or subdomain
  // with a DNS wildcard pointing to your server(s)
  staging: 'staging.example.com',
  // Should be a real registered domain or subdomain
  // with a DNS wildcard pointing to your server(s)
  prod: 'example.com'
};
